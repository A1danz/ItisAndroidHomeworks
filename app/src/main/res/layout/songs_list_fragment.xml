<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@color/white_bg"
    >

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_favorite_songs"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="200dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginTop="5dp"
        android:orientation="horizontal"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        tools:listitem="@layout/favorite_song_item"
        android:background="@android:color/transparent"
        android:padding="5dp"
        />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_songs"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/rv_favorite_songs"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        android:orientation="vertical"
        tools:listitem="@layout/song_item"
        />

    <androidx.appcompat.widget.LinearLayoutCompat
        android:id="@+id/layout_no_favorite_songs"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintStart_toStartOf="@id/rv_favorite_songs"
        app:layout_constraintEnd_toEndOf="@id/rv_favorite_songs"
        app:layout_constraintTop_toTopOf="@id/rv_favorite_songs"
        app:layout_constraintBottom_toBottomOf="@id/rv_favorite_songs"
        android:background="@color/black"
        android:alpha="0.8"
        android:visibility="invisible"
        >
        <TextView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:text="@string/favorite_songs_not_found"
            android:textColor="@color/white"
            android:fontFamily="@font/rubik"
            android:textAlignment="center"
            android:gravity="center"
            android:textSize="30sp"
            android:textStyle="bold"
            />
    </androidx.appcompat.widget.LinearLayoutCompat>
    <androidx.appcompat.widget.LinearLayoutCompat
        android:id="@+id/layout_songs_not_found"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintStart_toStartOf="@id/rv_songs"
        app:layout_constraintEnd_toEndOf="@id/rv_songs"
        app:layout_constraintTop_toTopOf="@id/rv_songs"
        app:layout_constraintBottom_toBottomOf="@id/rv_songs"
        android:background="@color/black"
        android:alpha="0.8"
        android:visibility="invisible"
        >
        <TextView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:text="@string/songs_not_found"
            android:textColor="@color/white"
            android:fontFamily="@font/rubik"
            android:textAlignment="center"
            android:gravity="center"
            android:textSize="30sp"
            android:textStyle="bold"
            />
    </androidx.appcompat.widget.LinearLayoutCompat>

</androidx.constraintlayout.widget.ConstraintLayout>